:80 {
	# Enable logging
	log {
		output stdout
		format console
	}

	# Proxy everything to the nginx frontend
	reverse_proxy frontend:8080 {
		transport http {
			read_timeout 120s
			write_timeout 120s
		}
	}
}
 